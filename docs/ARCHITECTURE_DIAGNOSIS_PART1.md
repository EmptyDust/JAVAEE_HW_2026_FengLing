# 微服务架构诊断报告：教师模块"寄生"问题

## 🚨 核心问题概述

你的担心是完全正确的。当前架构存在严重的**职责边界混乱**和**数据访问违规**问题，这将在实现 RBAC 权限管理时引发灾难性后果。

### 问题严重性评级：🔴 **P0 级别（阻塞性问题）**

---

## 📊 问题1：职责边界分析

### 当前架构的致命缺陷

```
┌─────────────────────────────────────────────────────────────┐
│                     当前架构（错误）                          │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  student-service          course-service        auth-service │
│  ┌──────────┐            ┌──────────────┐      ┌─────────┐ │
│  │ Student  │            │ TeacherInfo  │      │  User   │ │
│  │ Service  │            │   Service    │◄─────┤ Mapper  │ │
│  └──────────┘            └──────────────┘      └─────────┘ │
│       │                         │                    ▲       │
│       │                         │                    │       │
│       ▼                         ▼                    │       │
│  ┌──────────┐            ┌──────────────┐           │       │
│  │ student  │            │ teacher_info │           │       │
│  │  表      │            │     表       │           │       │
│  └──────────┘            └──────────────┘           │       │
│                                 │                    │       │
│                                 └────────────────────┘       │
│                          ❌ 跨服务直接访问数据库表            │
└─────────────────────────────────────────────────────────────┘
```

### 具体问题分析

#### 1.1 course-service 权限过大

**代码证据**：
```java
// TeacherInfoService.java:36
@Autowired
private UserMapper userMapper;  // ❌ course-service 直接访问 auth-service 的表

// TeacherInfoService.java:119-125
User user = new User();
user.setUsername(teacher.getTeacherNo());
user.setPassword(PasswordUtils.encode("123456"));
user.setUserType("teacher");
userMapper.insert(user);  // ❌ 跨服务直接操作 User 表
```

**问题**：
- course-service 拥有了创建用户账号的权限
- 违反了微服务的**单一职责原则**
- 违反了**数据库隔离原则**（每个服务应该只访问自己的数据库）

#### 1.2 与 student-service 的不一致性

**对比分析**：

| 维度 | student-service | course-service (教师) |
|------|----------------|---------------------|
| **服务职责** | ✅ 只管理学生信息 | ❌ 管理教师信息 + 创建用户账号 |
| **数据库访问** | ✅ 只访问 student 表 | ❌ 访问 teacher_info + user 表 |
| **账号创建** | ✅ 由 auth-service 负责 | ❌ 自己直接创建 |
| **架构一致性** | ✅ 符合微服务原则 | ❌ 严重违反 |

**代码对比**：
```java
// StudentService.java - 正确的做法
public void add(Student student) {
    studentMapper.insert(student);  // ✅ 只操作自己的表
}

// TeacherInfoService.java - 错误的做法
public TeacherInfo addTeacher(TeacherInfo teacher) {
    userMapper.insert(user);         // ❌ 操作其他服务的表
    teacherInfoMapper.insert(teacher);
}
```

---

## 📊 问题2：身份管理冲突

### 2.1 教师登录流程的混乱

**当前流程**：
```
教师登录
  ↓
auth-service 验证用户名密码
  ↓
生成 JWT Token (包含 userId, userType="teacher")
  ↓
❓ 如何获取教师的基本信息（姓名、部门、职称）？
  ↓
❌ 需要跨服务调用 course-service
```

**学生登录流程（正确）**：
```
学生登录
  ↓
auth-service 验证用户名密码
  ↓
生成 JWT Token (包含 userId, userType="student", studentId)
  ↓
✅ 可以直接从 student-service 获取学生信息
```

### 2.2 数据关联的不一致性

**User 表结构分析**：
```sql
CREATE TABLE user (
    id BIGINT PRIMARY KEY,
    username VARCHAR(50),
    user_type VARCHAR(20),  -- admin/teacher/student
    student_id BIGINT,      -- ✅ 学生有关联字段
    -- ❌ 没有 teacher_id 字段！
);
```

**问题**：
- User 表有 `student_id` 字段，可以关联学生信息
- User 表**没有** `teacher_id` 字段，无法关联教师信息
- 教师登录后，auth-service 无法知道对应的 teacher_info 记录

**代码证据**：
```java
// TeacherInfoService.java:128
teacher.setUserId(user.getId());  // ✅ teacher_info 有 user_id

// 但是 User 表没有 teacher_id！
// 导致从 User 无法反向查询 TeacherInfo
```

### 2.3 跨服务调用的复杂性

**场景**：教师登录后获取个人信息

**当前架构需要的调用链**：
```
1. 前端 → auth-service.login()
   返回：{ userId: 1, userType: "teacher" }

2. 前端 → course-service.getTeacherByUserId(userId)  ❌ 这个接口不存在！
   或者
   前端 → course-service.getTeacherList() 然后遍历查找？ ❌ 性能极差

3. 实际上无法高效获取教师信息
```

**学生架构的调用链（正确）**：
```
1. 前端 → auth-service.login()
   返回：{ userId: 1, userType: "student", studentId: 123 }

2. 前端 → student-service.getById(studentId)  ✅ 直接获取
```

---

## 📊 问题3：权限控制隐患

### 3.1 当前权限控制的严重缺陷

**代码证据**：
```java
// TeacherInfoController.java:24
@RequireRole({"admin"})  // ❌ 只有管理员可以操作
@RestController
@RequestMapping("/teacher")
public class TeacherInfoController {
    // 所有教师管理接口都只能管理员访问
}
```

**问题**：
- 教师无法修改自己的信息（如联系方式、头像）
- 教师无法查看自己的基本信息
- 所有教师操作都需要管理员介入

### 3.2 "教师只能修改自己课程的成绩" 的实现困境

**场景分析**：
```
需求：教师只能修改自己课程的成绩

当前架构的实现困境：
1. 成绩录入接口在 course-service
2. 需要验证：当前教师是否是该课程的授课教师
3. 需要查询：teacher_info 表（在 course-service）
4. 需要验证：当前登录用户的 userId 对应的 teacher_id

问题：
- User 表没有 teacher_id 字段
- 需要从 teacher_info 表反向查询 user_id
- 权限校验逻辑应该写在哪里？
```

**代码示例（当前无法实现）**：
```java
// CourseEnrollmentService.java:147
public void updateScore(Long enrollmentId, BigDecimal score, String grade) {
    // ❌ 如何验证当前教师是否有权限？
    // 1. 从 JWT 获取 userId
    // 2. 从 teacher_info 查询 teacherId (WHERE user_id = ?)
    // 3. 从 course_enrollment 查询 courseId
    // 4. 从 course_info 验证 teacherId 是否匹配

    // 问题：这个逻辑太复杂，而且需要多次数据库查询
}
```

### 3.3 循环依赖风险

**潜在的循环依赖**：
```
course-service 需要调用 auth-service 获取用户信息
    ↓
auth-service 需要调用 course-service 获取教师信息
    ↓
❌ 循环依赖！
```

**具体场景**：
1. 教师登录时，auth-service 需要返回教师的基本信息
2. auth-service 需要调用 course-service 的 getTeacherByUserId()
3. course-service 的权限拦截器需要验证用户身份
4. 权限拦截器需要调用 auth-service 验证 Token
5. ❌ 形成循环依赖

---

## 📊 问题4：数据一致性风险

### 4.1 "僵尸账号" 问题

**代码证据**：
```java
// TeacherInfoService.java:100-141
@Transactional(rollbackFor = Exception.class)
public TeacherInfo addTeacher(TeacherInfo teacher) {
    // 1. 创建用户账号
    userMapper.insert(user);  // ✅ 成功

    // 2. 设置教师信息的user_id
    teacher.setUserId(user.getId());

    // 3. 插入教师信息
    teacherInfoMapper.insert(teacher);  // ❌ 如果这里失败...
}
```

**问题分析**：

#### 场景1：teacher_info 插入失败
```
1. userMapper.insert(user) 成功 → user 表有记录
2. teacherInfoMapper.insert(teacher) 失败 → teacher_info 表无记录
3. @Transactional 回滚
4. ❌ 但是 user 表和 teacher_info 表可能在不同的数据库！
5. ❌ 跨数据库事务可能不生效
6. 结果：产生"僵尸账号"（有 User 记录，但没有 TeacherInfo 记录）
```

#### 场景2：并发创建冲突
```
线程A: 检查工号不存在 → 准备创建
线程B: 检查工号不存在 → 准备创建
线程A: 创建 User 成功
线程B: 创建 User 失败（用户名重复）
线程A: 创建 TeacherInfo 成功
线程B: 事务回滚，但 User 可能已经创建
```

### 4.2 数据库事务的局限性

**关键问题**：
```java
@Transactional(rollbackFor = Exception.class)
```

**这个注解的局限性**：
- 只能保证**同一个数据库**的事务一致性
- 如果 user 表和 teacher_info 表在不同的数据库，事务可能不生效
- 需要使用**分布式事务**（如 Seata）才能保证一致性

**当前架构的数据库分布**：
```
auth-service:
  - 数据库: student_system
  - 表: user

course-service:
  - 数据库: student_system (可能是同一个数据库)
  - 表: teacher_info, course_info

问题：
- 如果是同一个数据库，@Transactional 可以生效
- 但是违反了微服务的数据库隔离原则
- 如果未来拆分数据库，会立即出现数据一致性问题
```

### 4.3 删除教师的数据一致性问题

**代码证据**：
```java
// TeacherInfoService.java:178-198
@Transactional(rollbackFor = Exception.class)
public void deleteTeacher(Long id) {
    // 逻辑删除
    teacher.setStatus(0);
    teacherInfoMapper.updateById(teacher);

    // ❌ 但是 User 表的记录没有删除或禁用！
    // 结果：教师账号仍然可以登录
}
```

**问题**：
- 删除教师时，只更新了 teacher_info 表的 status
- User 表的记录没有处理
- 教师账号仍然可以登录系统
- 登录后会出现异常（找不到对应的 teacher_info 记录）

---

## 🎯 总结：架构问题的严重性

### 问题汇总

| 问题类型 | 严重性 | 影响范围 | 修复难度 |
|---------|--------|---------|---------|
| 职责边界混乱 | 🔴 P0 | 整个系统架构 | 高 |
| 数据库访问违规 | 🔴 P0 | 数据一致性 | 高 |
| 身份管理不一致 | 🔴 P0 | 用户体验 | 中 |
| 权限控制缺失 | 🔴 P0 | 安全性 | 中 |
| 数据一致性风险 | 🟡 P1 | 数据完整性 | 高 |
| 循环依赖风险 | 🟡 P1 | 系统稳定性 | 中 |

### 对 RBAC 权限管理的影响

**如果不修复这些问题，实现 RBAC 时会遇到**：
1. ❌ 无法实现"教师只能修改自己课程的成绩"
2. ❌ 无法实现"教师只能查看自己的课程列表"
3. ❌ 权限校验逻辑无处安放（写在哪个服务？）
4. ❌ 跨服务调用导致性能问题
5. ❌ 循环依赖导致系统无法启动

---

**下一部分：重构建议和今晚的应急方案**
